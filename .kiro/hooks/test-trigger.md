<!--
╔══════════════════════════════════════════════════════════════════════════════╗
║  📚 KIRO 学習ガイド: テスト連携 Hook                                         ║
╠══════════════════════════════════════════════════════════════════════════════╣
║                                                                              ║
║  このHookは「ソースコード変更 → テスト実行提案」の連携パターンです。         ║
║                                                                              ║
║  学習ポイント:                                                               ║
║  1. ソースとテストのマッピングテーブルの定義方法                             ║
║  2. Exclude パターンでテストファイル自体を除外                               ║
║  3. 複数のコマンドオプションを提案する方法                                   ║
║                                                                              ║
║  実践的な活用:                                                               ║
║  - TDD（テスト駆動開発）ワークフローの自動化                                 ║
║  - CI/CD パイプラインの事前チェック                                          ║
║                                                                              ║
║  詳細: KIRO_LEARNING.md の「5. Hooks（エージェント自動化）」を参照           ║
╚══════════════════════════════════════════════════════════════════════════════╝
-->

# Test Trigger on Source Change

## Description
ソースコード変更時に関連するテストの実行を提案します。

## Trigger
- **Event**: File saved
- **Patterns**:
  - `src/**/*.py`
  - `csv_ingest/app.py`
- **Exclude**: `**/test_*.py`

## Agent Instructions

ソースコードが変更されました。以下を確認・提案してください：

1. **対応するテストファイルの特定**
   | ソースファイル | テストファイル |
   |---------------|---------------|
   | `src/weather_handler.py` | `tests/test_weather_handler.py` |
   | `src/weather_service.py` | `tests/test_weather_service.py` |
   | `src/auth_service.py` | `tests/test_auth_service.py` |
   | `src/auth_middleware.py` | `tests/test_auth_middleware.py` |
   | `src/database.py` | `tests/test_database.py` |
   | `csv_ingest/app.py` | `csv_ingest/test_app.py` |

2. **テスト実行コマンドの提案**
   ```bash
   # 特定モジュールのテスト
   python -m pytest tests/test_<module>.py -v

   # プロパティベーステスト
   python -m pytest tests/ -v -m property

   # 全テスト
   python -m pytest tests/ -v
   ```

3. **テスト更新の必要性チェック**
   - 新しい関数/メソッドが追加された場合、テストが必要
   - 既存の関数シグネチャが変更された場合、テスト更新が必要
   - エッジケースが増えた場合、プロパティテスト追加を検討

4. **CSV Ingest の場合**
   ```bash
   cd csv_ingest && python3 test_app.py
   cd csv_ingest && python3 integration_test.py
   ```

## Reference Files
- `pytest.ini` - テストマーカー設定
- `tests/conftest.py` - pytest フィクスチャ
